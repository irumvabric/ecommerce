enum TextSize { small, medium, large }

enum OrderStatus { processing, Shipped, delivered, cancelled }

enum paymentMethods {
  visa,
  paypal,
  googlepay,
  applepay,
  razorpay,
  creditCard,
  masterCard,
  payStack,
  paytm,
}
